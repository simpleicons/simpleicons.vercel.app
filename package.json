{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "micro": "^9.3.4",
    "micro-fork": "^2.0",
    "my-way": "^1.0.2",
    "serve-marked": "^2.0.2",
    "simple-icons": "^1.18.0"
  }
}
