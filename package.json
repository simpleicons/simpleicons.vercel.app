{
  "private": true,
  "scripts": {
    "start": "micro index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "micro": "^10.0.1",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^5.0.0",
    "simple-icons": "^12.4.0"
  },
  "engines": {
    "node": "18.x"
  }
}
