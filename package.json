{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "@primer/octicons": "^17.4.0",
    "micro": "^9.4.0",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^3.1.2"
  },
  "engines": {
    "node": "16.x"
  }
}
