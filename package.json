{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "micro": "^9.3.4",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^2.0.2",
    "simple-icons": "^2.8.0"
  },
  "engines": { "node": "12.x" }
}
