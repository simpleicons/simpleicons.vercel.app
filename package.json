{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "micro": "^10.0.1",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^4.0.0",
    "simple-icons": "^8.7.0"
  },
  "engines": {
    "node": "18.x"
  }
}
