{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "micro": "^10.0.1",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^3.1.2",
    "simple-icons": "^9.2.0"
  },
  "engines": {
    "node": "18.x"
  }
}
